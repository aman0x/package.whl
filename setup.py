from setuptools import setup, find_packages

setup(
    name="keyward",
    version="0.1.0",
    packages=find_packages(),
    description="Data management operations wrapper",
    author="keyward",
    author_email="admin@keyward.io",
    python_requires=">=3.6",
)
